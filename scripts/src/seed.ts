// CoinCity seeding is performed by the API server on startup (see artifacts/api-server/src/lib/seed.ts).
// This script is intentionally a no-op so the workspace stays consistent.
console.log("Seeding is handled by the API server on startup.");
